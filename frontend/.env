VITE_API_BASE_URL=/api

# Audio Scribe transcription service (voice dictation on form textareas).
# Defaults to production if unset; local dev service runs at http://localhost:8000/api.
VITE_AUDIO_SCRIBE_API=https://dostscribe.dostcaraga.ph/api
